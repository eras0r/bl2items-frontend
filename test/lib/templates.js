define([
    'angular',
], function (angular) {

    'use strict';

    // each template used within tests needs to be included here
    require([
        'components/skill-tree/skill.html'
    ]);

});
